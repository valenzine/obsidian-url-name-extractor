{
	"name": "obsidian-url-name-extractor",
	"version": "2.0.1",
	"description": "Converts raw URLs into markdown links by automatically fetching webpage titles. Configurable regex patterns for URL detection and site-specific title extraction.",
	"main": "main.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/valenzine/obsidian-url-name-extractor.git"
	},
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"url",
		"title",
		"extractor"
	],
	"author": "valenzine",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"builtin-modules": "^5.0.0",
		"esbuild": "^0.27.2",
		"obsidian": "latest",
		"tslib": "^2.8.0",
		"typescript": "^5.9.0"
	}
}
